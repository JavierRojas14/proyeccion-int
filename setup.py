from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='This is the repository to analyze the projection and measure how big the new INT is gonna be',
    author='Javier Rojas',
    license='MIT',
)
